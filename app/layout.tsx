import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Splito - Split Expenses With Friends',
  description: 'Easily split bills, track payments, and manage group expenses all in one place.',
  keywords: 'expense splitting, bill splitting, group expenses, payment tracking',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

